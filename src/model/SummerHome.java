package model;

import util.CurrencyType;

public class SummerHome extends Build{

    public SummerHome(double squareMeter, int numberOfRooms, int numberOfHalls, double price, CurrencyType currencyType) {
        super(squareMeter, numberOfRooms, numberOfHalls, price, currencyType);
    }
}
